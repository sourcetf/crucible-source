# Contact / Coordination File

## Session 1008 (2026-09-12) — Worker 1008

### Current State (Post-Crash Analysis)
- **cargo check status**: 115 errors (was 47 warnings) after `--features tls,tls_boring`
- **Critical blockers**:
  1. **Missing import**: `h2` crate not in Cargo.toml deps but imported by `src/server/h2.rs`
  2. **Missing import**: `quinn`crate not in deps (workspace only has `quinn-boring` fork)
  3. **geoip_panel incomplete**: `GeoLine` struct missing, `upsert_edit`/`upsert_cron` undefined
  4. **rustls API mismatch**: v0.23 removed `with_default_provider`, `curve_descriptors`, `.context()` on iterators
  5. **sidecar_engine**: `handle_with_fallback` not exported (used but undefined)
  6. **tar crate**: wrong crate name for bindings.rs
  7. **app_ffi**: `mut` binding error on `child`

### Build Environment
- OpenBSD 7.5: libclang.so not available → bindgen fails
- Need to either: (a) use prebuilt bindings, or (b) use rustls feature only for build

### Fix Plan (Morning Build Target)

#### Phase 1: Cargo.toml Repair (local fix → push & build remote)
1. Add `h2 = { path = "libs/h2" }` to [dependencies]
2. Check quinn-boring binding for `quinn` export alias
3. Add `tar` crate for bindings.rs
4. Fix `fastrand` issue if needed

#### Phase 2: geoip_panel Stub Completion (local fix → push)
1. Add missing `GeoLine` struct to `mod.rs`
2. Implement `upsert_edit`, `upsert_cron` in `ops.rs`
3. Fix `body` module import (http-body-util vs http-body)

#### Phase 3: TLS/QUIC API Fixes (local fix → push)
1. rustls 0.23: replace `with_default_provider()` with `with(_provider)` 
2. Remove `.context()` on cert/key iterator calls, use `.next().unwrap()` pattern
3. Fix `PrefixedStream` `AsyncWrite` trait bound (generic over Vec<u8>)
4. `mut self` pinning issue in app_ffi

#### Phase 4: Build Verification (remote)
1. `unset CARGO_TARGET_DIR && cargo build --release --features tls,tls_boring`
2. If bindgen fails, fallback to `--features tls` (boring=disabled)
3. Verify binary 16-18MB, runs `./target/release/webserver --config config.toml`

#### Phase 5: Smoke Test (remote)
1. Ports: 9081 plain, 9445 TLS1.2, 9446 TLS1.3, 8443 prod, 9095 apps
2. HTTP regression: h1/h2/h3 plain/TLS
3. DNS test: nc -u 53, dig @127.0.0.1 example.com A
4. Apps test: curl :9095/rust, :9095/php

### Coordination Notes
- All fixes will be made on remote `/crucible` via SSH
- Push with `git add -A && git commit -m "Session 1008: fix cargo imports, geoip_panel stubs, rustls 0.23 API, app_ffi mut" && git push origin v1.0.0-final`
- After push: `scripts/build_release.sh --restart`
- Report progress to this file

---

## Session 1005-1006 Baseline (for reference)
- ✅ remote server (83.229.125.81) 构建成功
- ✅ Binary: /crucible/target/release/webserver (16.2MB, Sep 9 07:49)
- ✅ Git 推送完成 (commit 6a5306d)

### 已实现功能
#### HTTP 基础
- ✅ HTTP/1.1 (hyper 1.x) - h1.rs
- ✅ HTTP/2 (h2.rs) - BATCH_CAP=16, max_send_buffer 128KB
- ✅ HTTP/3 (h3.rs) - quinn-boring + h3-qmux, RFC 9298 CONNECT-UDP

#### TLS/SSL
- ✅ BoringSSL 主路径 + ClientHello 分流
- ✅ TLS 1.2/1.3 全支持
- ✅ ECH 自动配置 (ech_auto.rs)
- ✅ OCSP 智能获取 (ocsp_fetcher.rs)
- ✅ PQC 后量子 (X25519MLKEM768)
- ✅ PSK 支持

#### 应用引擎
- ✅ PHP-FPM (优先) + php-cgi 回退
- ✅ Lua FFI (libapp_lua.so)
- ✅ C/Go/Rust 原生插件 (libapp_{c,go,rust}.so)
- ✅ WSGI/ASGI/PSGI/Rack
- ✅ JSP/Jetty sidecar (UDS)
- ✅ ASP/ASP.NET FFI
- ✅ Script FFI (Python/Ruby/Perl 静态嵌入)

#### DNS (BIND9 后端)
- ✅ 权威/递归/根服务器三模式
- ✅ DNSSEC 自动签名 + ZSK/KSK 周期轮换
- ✅ NSEC3 (默认启用)
- ✅ DoT/DoH 加密 DNS
- ✅ ECS (EDNS Client Subnet)
- ✅ AXFR 增量更新
- ✅ GeoIP 路由

#### Admin UI
- ✅ 嵌入式 admin_ui.html (include_str!)
- ✅ 热配置重载
- ✅ TLS 配置

### 已知问题/后续改进
- named 偶发 futex_noasync 崩溃 (容器内核限制)
- rndc key 不匹配 (时钟/容器环境问题)