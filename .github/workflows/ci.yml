name: CI-test
on:
  push:
    branches: [main]
jobs:
  test:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - run: echo "checkout OK"
      - run: rustc --version
      - run: cargo --version
